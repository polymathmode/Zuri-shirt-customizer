{
  "name": "apps",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd apps/server && node index.js",
    "build": "cd apps/client && yarn install && yarn build"
  },
 
  "engines": {
    "node": ">=14.0.0"
  }
}
