{
  "name": "apps",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node index.js",
    "build": "cd client && yarn install && yarn build",
    "dev": "nodemon index.js"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
