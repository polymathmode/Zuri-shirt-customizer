{
  "name": "apps",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd apps/server && yarn install && node index.js",
    "build": "cd apps/server && yarn install && cd ../client && yarn install && yarn build"
  },
  
  "engines": {
    "node": ">=14.0.0"
  }
}


